<div class="col-md-2 my-5">
  <table class="table my-4">
    <thead class="table">
      <tr><th>ジャンル検索</th></tr>
    </thead>
    <% genres.each do |genre| %>
      <tr>
        <td class="color-dark"><%= link_to genre.name, url: "#" %> </td>
      </tr>
    <% end %>
  </table>
</div>